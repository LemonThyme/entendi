{
  "name": "entendi",
  "version": "0.1.0",
  "description": "Comprehension accountability layer for AI-assisted work",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && node --import tsx esbuild.config.ts",
    "build:hooks": "node --import tsx esbuild.config.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "esbuild": "^0.27.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
